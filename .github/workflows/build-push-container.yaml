name: Build Push Containers

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
    tags:
      - 'v*'
  pull_request:
    branches:
      - 'main'

jobs:
  release-helm:
    uses: gccloudone-aurora/.github/.github/workflows/release-helm.yaml@v1
